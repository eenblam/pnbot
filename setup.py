from setuptools import setup, find_packages

setup(
        name='pnbot',
        version='0.0.1',
        packages=find_packages(),
        install_requires=[
            'discord.py',
            ],
        entry_points={},
        )
